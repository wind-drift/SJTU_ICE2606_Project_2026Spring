% !TeX encoding = UTF-8
% !TeX root = ../main.tex

\section{整体算法流程}

\begin{frame}{整体算法流程与信号处理关联}
  \small
  \centering

  \begin{tikzpicture}[
    stage/.style={
      draw, rounded corners=3pt,
      minimum height=1.05cm,
      align=center,
      font=\small,
      text width=3.5cm
    },
    arr/.style={->, >=stealth, thick}
  ]
    \node[stage, fill=blue!10] (pre) {
      \textbf{预处理}\\
      语音输入 $\to$ 端点检测 $\to$ 预加重
    };

    \node[stage, fill=green!10, right=0.85cm of pre] (feat) {
      \textbf{特征提取}\\
      分帧加窗 $\to$ FFT $\to$ Mel $\to$ MFCC
    };

    \node[stage, fill=orange!14, right=0.85cm of feat] (cls) {
      \textbf{分类预测}\\
      KNN / GMM / CNN $\to$ 数字 0--9
    };

    \draw[arr] (pre) -- (feat);
    \draw[arr] (feat) -- (cls);
  \end{tikzpicture}

  \vspace{0.45cm}

  \begin{columns}[T,totalwidth=\textwidth]
    \column{0.47\textwidth}
    \begin{block}{两条技术路线}
      \footnotesize
      \begin{itemize}
        \setlength{\itemsep}{1pt}
        \item \textbf{传统方法}：MFCC 统计特征 / 序列特征 + KNN / GMM
        \item \textbf{深度方法}：MFCC 特征图 + TinyKeywordCNN
      \end{itemize}
    \end{block}

    \column{0.49\textwidth}
    \begin{block}{信号与系统核心关联}
      \footnotesize
      \begin{itemize}
        \setlength{\itemsep}{1pt}
        \item FFT / STFT：频域分析与时频联合分析
        \item 预加重 / Mel 滤波：FIR 滤波与非线性频率映射
        \item DCT：倒谱去相关与特征压缩
      \end{itemize}
    \end{block}
  \end{columns}
\end{frame}